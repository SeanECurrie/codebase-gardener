name: CI

on:
  push:
    branches: [main, development, mvp/focus-cli]
  pull_request:
    branches: [main, development]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev] || pip install -e .

      - name: Lint with ruff
        run: ruff check

      - name: Format check with ruff
        run: ruff format --check

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev] || pip install -e .

      - name: Run unit tests
        run: pytest -q -m "not integration and not e2e"

      - name: Check task execution blocks (warning only)
        run: |
          if find tasks/ -name "task-*.md" -exec grep -L "Execution Block" {} + 2>/dev/null | head -1; then
            echo "::warning::Some task files missing Execution Block"
          fi
        continue-on-error: true

  integration-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev] || pip install -e .

      - name: Run integration tests
        run: pytest -q -m "integration"
